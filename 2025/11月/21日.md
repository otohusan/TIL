# 2025 年 11 月 21 日

## ReturnType

「関数の型」から「戻り値の型」を取り出すことができる

```ts
function greet(name: string): string {
  return `Hello, ${name}`;
}
```

これの型は 以下の通り

```ts
typeof greet; // => (name: string) => string;
```

ReturnType を使えば 戻り値である **string** を取得する

```ts
ReturnType<typeof greet>; // => string
```

### 余談

引数の型を取得するのは `Parameters<T>`

```ts
Parameters<typeof greet>; // => [name: string]
```

## 間違って reset --hard してしまった

git reflog が使える

リセットによって消えてしまったコミットも見ることができるので、

`git reset --hard HEAD@{特定のコミットまでの数字}` みたいな感じで指定してあげれば良い
